version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: doc/conf.py

python:
  install:
    - method: pip
      path: .
    - method: pip
      requirements: doc/requirements-rtd.txt
